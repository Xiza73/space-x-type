# Claude Code — personales
CLAUDE.local.md
.claude/settings.local.json

# Dependencias
node_modules/

# Solo bun.lock se commitea — el resto de lockfiles no deberían existir
package-lock.json
pnpm-lock.yaml
yarn.lock

# Build
dist/
dist-ssr/
src-tauri/target/
src-tauri/gen/

# Referencia de diseño — local, no se trackea
design/

# Assets generados / caché local
*.local
audio-cache/
beatmaps-cache/

# Entorno
.env
.env.*
!.env.example

# Editores / SO
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Sidecars: se bajan al construir con `bun run tools`, no se versionan.
src-tauri/binaries/

# audio del banco de tempo: decenas de MB con derechos
src-tauri/bench/audio/
